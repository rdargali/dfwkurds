// NOTE: This file is a disabled copy of the revalidation API route.
// It's intentionally renamed to avoid inclusion during static export.
// To re-enable dynamic revalidation on a Node server or Vercel, restore
// this file to `src/app/api/revalidate/route.ts`.

/*
import { revalidatePath } from 'next/cache'
import { NextRequest, NextResponse } from 'next/server'

export async function POST(request: NextRequest) {
  // (original revalidation logic removed for static export)
  return NextResponse.json({ message: 'Revalidation endpoint disabled for static export' })
}

export async function GET(request: NextRequest) {
  return NextResponse.json({ message: 'Revalidation endpoint disabled for static export' })
}
*/
